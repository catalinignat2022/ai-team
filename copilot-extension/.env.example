# GitHub Copilot Extension Environment Configuration
# Copy this file to .env and fill in your actual values

# GitHub App Configuration
GITHUB_APP_ID=your_github_app_id_here
GITHUB_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nyour_private_key_here\n-----END PRIVATE KEY-----"
GITHUB_WEBHOOK_SECRET=your_webhook_secret_here
GITHUB_INSTALLATION_ID=your_installation_id_here

# Anthropic Claude Configuration  
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Server Configuration
PORT=3000
NODE_ENV=development

# Security Configuration
SESSION_SECRET=your_secure_random_session_secret_here

# Optional: Logging Configuration
LOG_LEVEL=info

# Optional: Database Configuration (if needed for future features)
# DATABASE_URL=postgresql://username:password@localhost:5432/devops_extension

## Setup Instructions:

### 1. GitHub App Creation
# Visit: https://github.com/settings/apps/new
# App Name: Romanian Dating DevOps Copilot Extension
# Homepage URL: https://your-domain.com (or localhost for dev)
# Webhook URL: https://your-domain.com/webhook (or ngrok URL for dev)
# Webhook Secret: Generate a secure random string
# Permissions needed:
#   - Repository: Read & Write
#   - Pull requests: Read & Write  
#   - Issues: Read & Write
#   - Actions: Read & Write
#   - Contents: Read & Write

### 2. Anthropic API Key
# Visit: https://console.anthropic.com/
# Create account and generate API key
# Ensure you have access to Claude Sonnet 4

### 3. Local Development Setup
# Install ngrok for webhook testing: npm install -g ngrok
# Run: ngrok http 3000
# Use the https URL as your GitHub App webhook URL

### 4. GitHub App Installation
# After creating the app, install it on your repositories
# Note the installation ID from the URL

### 5. Environment Variables
# Generate session secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Generate webhook secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
